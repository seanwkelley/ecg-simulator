import React from "react";
import ReactDOM from "react-dom/client";
import ECGSimulatorGuide from "./ECGSimulatorGuide.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ECGSimulatorGuide />
  </React.StrictMode>
);
